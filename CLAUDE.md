# 작업 방식

## 먼저 `docs/map.md`를 읽는다

새 세션은 대화 기록이 없다. 코드베이스를 광범위하게 스캔(`Glob`/`Grep`
전수 탐색)하거나 커밋 로그를 훑어 맥락을 재구성하기 전에, 먼저
`docs/map.md`를 읽는다 — 파일별 역할, 데이터가 어디 있는지, 자주
헷갈리는 흐름(`tx()` 파이프라인·동기화·서버 시계·결과 기록 강제 등),
최근 설계 결정의 위치가 정리돼 있다. 거기 없는 것만 찾아서 연다.

기능을 새로 추가하거나 기존 흐름을 바꿨으면 `docs/map.md`의 해당 항목도
함께 고친다 — 표는 코드 주석을 옮겨 적지 않고 **위치만** 가리키므로,
표를 최신으로 유지하는 비용은 항목 하나 추가/수정뿐이다.

## 테스트는 기본적으로 돌리지 않는다

토큰 소모가 크다. 기본은 **문법 검사(`node --check`)까지**다.
브라우저 테스트(scratchpad의 `*-test.js`)는 **물어보고** 돌린다.

예외가 하나 있다. **`firestore.rules`를 고쳤으면 에뮬레이터 규칙 테스트는
묻지 않고 돌린다.** 그 파일은 사용자가 콘솔에 직접 붙여넣고, 틀리면
소유자·운영자가 자기 동호회에서 잠긴다. 되돌리는 비용이 테스트 비용보다
훨씬 크다.

기능을 없앨 때는 그것을 검사하던 테스트도 같이 정리한다. 안 그러면
다음에 돌릴 때 "없앤 기능이 없다"는 실패가 진짜 회귀를 가린다.

## 배포

- 자산 버전은 `public/index.html`·`public/manual.html`의 `?v=`와
  `state.js`의 `APP_VERSION`을 **함께** 올린다. 하나만 올리면
  `index.html`이 캐시된 기기에서 새 코드가 안 뜬다.
- `firestore.rules`는 사용자가 콘솔에 직접 붙여넣는다. 규칙이 바뀌면
  머지 전에 알린다.
- 배포 확인은 GitHub Actions API로 한다. 이 샌드박스에서는 프록시가
  `happybirdies.web.app` 접속을 403으로 막아 curl로 확인할 수 없다.
