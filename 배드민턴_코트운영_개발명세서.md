# 배드민턴 클럽 코트 운영 시스템 개발명세서

**문서 버전** v1.3
**작성일** 2026-08-10
**대상** 회원 50명 규모 배드민턴 동호회 / 3코트 운영

**변경 이력**

| 버전 | 일자 | 내용 |
|---|---|---|
| v1.0 | 2026-08-10 | 최초 작성 |
| v1.1 | 2026-08-10 | 성별 필수화, 경기 유형(남복/여복/혼복) 자동 판정 및 수동 선택, 성별 매칭 정책 추가 |
| v1.2 | 2026-08-10 | 실제 클럽 성비(남29·여12) 시뮬레이션 반영. 기본 정책을 `FREE`로 변경, 3:1 감점을 정책과 분리하고 공정성 예외 규칙 추가 (부록 D) |
| v1.3 | 2026-08-10 | 구현 중 발견한 두 가지 결함 반영: 자동화 실행 **순서** 고정(§5.7), **풀 최소 확보 인원** 도입(§5.4). 부록 E에 구현본 실측 추가 |

---

## 1. 개요

### 1.1 배경

현재 자석 마그넷 + 화이트보드로 대진을 운영하고 있으며 다음 문제가 있다.

| 현행 문제 | 해결 방향 |
|---|---|
| 자석 자력이 강해 이동이 어렵다 | 터치/드래그로 즉시 이동 |
| 네임펜 이름이 뭉개진다 | DB에 회원 저장, 항상 선명한 텍스트 |
| 신규 회원마다 동전 제작 필요 | 회원 등록 화면에서 1분 내 추가 |
| 누가 몇 게임 쳤는지 기억에 의존 | 게임 수 자동 카운트 |
| 매번 사람이 다음 팀을 짜야 한다 | 반자동 배치 + 언제든 수동 개입 |

### 1.2 목표

- 체육관 벽 태블릿(유휴 기기)에 띄워두고 **인터넷 없이도** 돌아가는 대진 운영 화면
- 개입이 없으면 **자동으로** 대기 → 코트 투입 → 종료 → 대기 복귀가 순환
- 개입하면 **완전 수동**으로 아무 자리에나 아무나 배치 가능
- 회원 명단(이름/출생년도/급수/성별)은 DB에 상주, 매번 입력 불필요
- 4인 성별 구성에 따라 **남복/여복/혼복이 자동 판정·표기**되고, 필요하면 수동으로 바꿀 수 있다

### 1.3 비목표 (v1 범위 제외)

- 회비/출결 정산, 점수(스코어) 기록, 리그전 순위표
- 개인 계정 로그인 / 권한 분리 (v1은 체육관 내 공용 단말 신뢰 모델)
- 개인 스마트폰에서의 원격 출석 (v2 검토)

---

## 2. 용어 정의

| 용어 | 정의 |
|---|---|
| **회원(Member)** | DB에 영구 저장되는 클럽 구성원. 이름/출생년도/급수/성별 보유 |
| **출석자(Attendee)** | 당일 세션에 출석 체크된 회원. 당일 게임 수 등 세션 상태 보유 |
| **풀(Pool)** | 출석했으나 아직 어느 팀에도 속하지 않은 대기 인원 집합 |
| **대기 슬롯(Queue Slot)** | 코트 투입을 기다리는 4인 팀 자리. 총 7개(Q1~Q7), Q1이 최우선 |
| **코트(Court)** | 1·2·3번 코트. 각 4인 + 2:2 팀 분할 정보 보유 |
| **리벤지(Revenge)** | 방금 끝낸 4인이 멤버 그대로 다시 대기 슬롯에 들어가는 것 |
| **락(Lock)** | 사람이 수동 지정한 슬롯/코트를 자동 로직이 건드리지 못하게 잠금 |
| **세션(Session)** | 운동일 1일 단위 운영 단위. 날짜 기준으로 생성/마감 |
| **경기 유형(MatchType)** | 4인의 성별 구성과 팀 분할로 결정되는 경기 종류. `MD`(남복) / `WD`(여복) / `XD`(혼복) / `MX`(혼성) |
| **혼복(XD)** | 남2·여2이며 **각 팀이 남1+여1**로 나뉜 정식 혼합복식 |
| **혼성(MX)** | 성별이 섞였지만 정식 혼복 구성이 아닌 경우. 남3여1, 남1여3, 또는 남2여2인데 남남 vs 여여로 나뉜 경우 |
| **성별 정책(GenderPolicy)** | 자동 배치 시 성별을 어떻게 고려할지 정하는 전역 설정 (§5.6) |

---

## 3. 사용자 및 이용 시나리오

### 3.1 사용자 유형

| 유형 | 설명 | 주 사용 화면 |
|---|---|---|
| 일반 회원 | 도착 시 자기 이름 터치 | 출석 화면 |
| 운영자(총무/게임 담당) | 대진 조정, 예외 처리 | 운영 대시보드 |
| 관리자 | 회원 추가/수정/급수 변경 | 관리 화면 |

> v1은 역할 분리를 강제하지 않는다. 관리 화면 진입에만 4자리 PIN을 둔다.

### 3.2 대표 시나리오

**S1. 도착 → 출석**
회원이 체육관 도착 → 벽 태블릿의 출석 화면에서 자기 이름 카드 터치 → 카드가 초록으로 전환 → 자동으로 풀(Pool)에 합류 → 대기 슬롯에 빈자리가 있으면 자동 배정

**S2. 코트 비었을 때 자동 투입**
1코트 게임 종료 버튼 터치 → 4명이 풀로 복귀 → 1코트가 비면 Q1 팀이 자동으로 1코트에 투입 → Q2~Q7이 한 칸씩 앞으로 당겨짐 → 맨 뒤 슬롯이 풀에서 자동 충원

**S3. 리벤지**
게임 종료 시 나오는 다이얼로그에서 [리벤지] 선택 → 4명이 그대로 대기 슬롯 맨 뒤에 팀 단위로 등록됨

**S4. 수동 개입**
운영자가 김철수 칩을 Q3 슬롯의 빈 자리로 드래그 → 해당 슬롯 자동 잠김(Lock) → 자동 로직은 그 슬롯을 건드리지 않음

**S5. 3명만 찬 슬롯**
Q5에 3명만 있고 풀에 사람이 있으면 → 자동 충원 규칙에 따라 1명을 채워 4명 완성 → 4명이 되어야만 코트 투입 대상이 됨

**S6. 경기 유형 자동 표기**
Q2에 남자 4명이 채워짐 → 슬롯 헤더에 **남복** 배지 자동 표시 → 여자 4명이면 **여복** → 남2·여2가 채워지면 팀 분할을 남1여1로 자동 조정하고 **혼복** 배지 표시

**S7. 경기 유형 수동 변경**
남2·여2로 채워진 Q3의 배지를 터치 → [남복][여복][혼복][혼성] 중 선택 팝업 → **혼성**(남남 vs 여여) 선택 → 팀 분할이 남자팀 vs 여자팀으로 재구성되고 슬롯 잠김

---

## 4. 기능 요구사항

### 4.1 회원 관리 (F-100)

| ID | 기능 | 상세 |
|---|---|---|
| F-101 | 회원 등록 | 이름, 출생년도(4자리), 급수, **성별(필수)**, 활성여부 |
| F-102 | 회원 수정/비활성 | 삭제 대신 `is_active=false` (기록 보존) |
| F-103 | 급수 체계 설정 | 기본: S(자강)/A/B/C/D/E(초심). 관리 화면에서 라벨·가중치 편집 가능 |
| F-104 | 일괄 등록 | CSV 붙여넣기 (`이름,출생년도,급수,성별`) 로 초기 50명 한 번에 입력 |
| F-105 | 백업/복원 | 전체 데이터 JSON export / import |
| F-106 | 성별 필수 검증 | 성별은 `M`/`F` 중 하나로 필수 입력. 미입력 회원은 등록 저장 불가 |
| F-107 | 성별 누락 보정 | 기존 데이터 이관 등으로 성별이 비어 있으면 관리 화면 상단에 경고 배너와 대상 목록 표시. 해당 회원은 출석은 되지만 경기 유형 판정에서 `UNKNOWN`으로 처리 |
| F-108 | 성별 필터 | 회원 목록 및 출석 화면에서 남/여 필터 제공 |

**급수 가중치(밸런싱용 기본값)**

| 급수 | 라벨 | 가중치 |
|---|---|---|
| S | 자강 | 6 |
| A | A조 | 5 |
| B | B조 | 4 |
| C | C조 | 3 |
| D | D조 | 2 |
| E | 초심 | 1 |

### 4.2 출석 관리 (F-200)

| ID | 기능 | 상세 |
|---|---|---|
| F-201 | 출석 토글 | 회원 카드 터치로 출석/미출석 전환. 터치 타깃 최소 88×88px |
| F-202 | 정렬/검색 | 가나다순 / 급수순 / 최근 출석순 토글, 초성 검색바 |
| F-203 | 출석 해제 | 이미 코트·대기에 배치된 사람을 해제하려 하면 확인 다이얼로그 후 모든 배치에서 제거 |
| F-204 | 게스트 추가 | 이름 + 성별 + (선택)급수를 입력하는 임시 인원(당일 세션 한정, DB 미등록). 성별은 경기 유형 판정에 필요하므로 필수 |
| F-205 | 일괄 퇴장 | 세션 마감 시 전원 미출석 처리 |

### 4.3 코트 운영 (F-300)

| ID | 기능 | 상세 |
|---|---|---|
| F-301 | 코트 3면 표시 | 각 코트는 2:2 팀 구조(팀A 2자리 / 팀B 2자리)로 시각화 |
| F-302 | 경기 시작 | 4명이 차면 [시작] 활성. 시작 시각 기록, 경과 타이머 표시 |
| F-303 | 경기 종료 | [종료] 터치 → 후속 처리 다이얼로그(아래) |
| F-304 | 종료 후 처리 | ① 전원 풀 복귀(기본) ② 리벤지(4인 팀 유지→대기 맨뒤) ③ 팀 유지 부분 선택(2명만 유지) |
| F-305 | 코트 수 설정 | 3면 기본, 설정에서 1~6면 변경 가능 |
| F-306 | 코트 비활성 | 특정 코트 사용 불가 시 "사용 안 함" 처리 → 자동 투입 대상 제외 |
| F-307 | 경기 시간 경고 | 설정값(기본 18분) 초과 시 코트 카드 테두리 색 변경 |
| F-308 | **경기 유형 자동 판정** | 4인이 채워지는 즉시 §5.6 규칙으로 남복/여복/혼복/혼성을 판정해 코트 카드에 배지 표시 |
| F-309 | **경기 유형 수동 선택** | 배지를 터치하면 선택 팝업. 현재 성별 구성으로 가능한 유형만 활성화되고, 선택 시 팀 분할이 그에 맞게 재구성됨 |
| F-310 | 유형별 기록 | 경기 종료 시 확정된 `match_type`을 `matches`에 저장 (통계용) |

### 4.4 대기열 운영 (F-400)

| ID | 기능 | 상세 |
|---|---|---|
| F-401 | 7개 슬롯 표시 | Q1~Q7. Q1이 다음 투입 순번 |
| F-402 | 자동 충원 | 빈자리가 생기면 §5 알고리즘으로 풀에서 자동 채움 |
| F-403 | 슬롯 당김 | 코트 투입으로 Q1이 비면 Q2→Q1으로 순차 이동 |
| F-404 | 슬롯 순서 변경 | 슬롯 카드 자체를 드래그해 순서 재정렬 |
| F-405 | 슬롯 잠금 | 수동 편집된 슬롯은 자동 잠금. 자물쇠 아이콘 토글로 해제 가능 |
| F-406 | 슬롯 비우기 | 슬롯 전원을 풀로 되돌리기 |
| F-407 | 우선 투입 | 슬롯을 코트로 직접 드래그하면 순번 무시하고 즉시 투입 |
| F-408 | **슬롯 유형 표시** | 4명이 찬 슬롯에 경기 유형 배지 표시. 인원이 바뀌면 즉시 재판정 |
| F-409 | **슬롯 유형 지정(핀)** | 슬롯에 유형을 미리 고정할 수 있다. 예: Q4를 "여복"으로 지정하면 자동 충원이 여성만 채운다. 해당 성별 인원이 부족하면 슬롯을 미완성 상태로 두고 다음 슬롯으로 넘어간다 |
| F-410 | 유형 핀 해제 | 핀 아이콘 재터치로 해제 → 일반 자동 충원 대상으로 복귀 |

### 4.5 배치 조작 (F-500)

| ID | 기능 | 상세 |
|---|---|---|
| F-501 | 드래그앤드롭 | 풀↔슬롯↔코트 간 개인 이동, 슬롯↔슬롯 팀 이동 |
| F-502 | 탭-투-무브 | 대체 조작: 인원 탭 → 목적지 탭 (드래그 실패 대비, 접근성) |
| F-503 | 스왑 | 이미 사람이 있는 자리에 드롭하면 두 사람 위치 교환 |
| F-504 | 실행 취소 | 최근 20개 액션 되돌리기 (Undo/Redo) |
| F-505 | 자동 재배치 | [자동 정렬] 버튼: 잠기지 않은 모든 슬롯을 알고리즘으로 재구성 |
| F-506 | 자동 모드 토글 | 전역 스위치. OFF면 어떤 자동 배치도 실행되지 않음 |
| F-507 | 팀 분할 교체 | 4인 내에서 2:2 분할만 바꾸는 [팀 바꾸기] 버튼. 3가지 분할을 순환하며, 각 분할의 경기 유형이 함께 갱신됨 |

### 4.6 통계·기록 (F-600)

| ID | 기능 | 상세 |
|---|---|---|
| F-601 | 개인 게임 수 | 칩에 당일 게임 수 배지 표시 |
| F-602 | 대기 시간 | 마지막 경기 종료 후 경과 시간 표시 (10분 초과 시 강조) |
| F-603 | 경기 이력 | 세션별 경기 기록(참가 4인, 시작/종료 시각) 저장 |
| F-604 | 세션 요약 | 마감 시 인원별 게임 수, 총 경기 수, 평균 경기 시간 |
| F-605 | 파트너 이력 | 최근 N게임 내 같은 조 편성 횟수 (중복 회피용 내부 데이터) |
| F-606 | 유형별 집계 | 세션 요약에 남복/여복/혼복/혼성 경기 수와 개인별 유형 분포 표시 |

---

## 5. 자동 배치 알고리즘

### 5.1 설계 원칙

1. **공정성 최우선** — 게임 수가 적은 사람이 먼저 나간다.
2. **대기 시간 보정** — 같은 게임 수라면 오래 쉰 사람이 먼저.
3. **중복 회피** — 방금 같이 친 사람과 연속으로 다시 묶이지 않게.
4. **밸런스** — 팀 급수 합이 비슷하게.
5. **성별 구성** — 남3여1처럼 애매한 구성은 강하게 피하되, 그것 때문에 누군가가 못 치게 되면 감점을 푼다.
6. **예측 가능성** — 사람이 "왜 저 사람이 먼저지?"를 물으면 설명 가능해야 한다.

### 5.2 개인 우선순위 점수

풀에 있는 각 인원 `p`에 대해:

```
priority(p) = W_GAME × (maxGames - p.gameCount)
            + W_WAIT × min(p.waitMinutes, 30)
            + W_NEW  × (p.gameCount == 0 ? 1 : 0)
```

기본 가중치 (설정에서 조정 가능)

| 상수 | 기본값 | 의미 |
|---|---|---|
| `W_GAME` | 100 | 게임 수 격차 1당 100점. 사실상 1순위 |
| `W_WAIT` | 3 | 대기 1분당 3점 (최대 90점, 게임 1개 차이를 넘지 않음) |
| `W_NEW` | 40 | 아직 한 게임도 못 친 사람 가산 |

### 5.3 4인 조합 선택

1. 풀을 `priority` 내림차순 정렬 → 상위 **K명**(기본 K=10)을 후보군으로 자른다.
2. 후보군에서 4인 조합을 전수 탐색한다. (C(10,4) = 210 → 연산 부담 없음)
3. 각 조합의 점수:

```
score(combo) = Σ priority(p)                        // 공정성
             - W_REPEAT × repeatPenalty(combo)      // 중복 회피
             - W_BAL    × balanceCost(combo)        // 급수 밸런스
             - W_AGE    × ageCost(combo)            // 연령 편차 (선택)
             + genderTerm(combo)                    // 3:1 감점 + 성별 정책 (§5.5)
```

**repeatPenalty** — 조합 내 모든 2인 쌍에 대해, 최근 `R`게임(기본 3) 이내에 같은 코트에 있었으면 쌍당 1점 누적. `W_REPEAT = 25`

**balanceCost** — 조합을 2:2로 나누는 3가지 분할 중, **경기 유형 제약을 만족하는 분할들** 가운데 급수 가중치 합 차이의 최솟값. `W_BAL = 10`

```
분할1: (1,2) vs (3,4)
분할2: (1,3) vs (2,4)
분할3: (1,4) vs (2,3)
balanceCost = min(|합A - 합B|)  → 이때의 분할을 팀 편성으로 채택
```

> 남2·여2 조합에서 목표 유형이 **혼복(XD)** 이면 3가지 분할 중 각 팀이 남1여1이 되는 **2가지만** 후보가 된다. 그 2가지 중 급수 합 차이가 작은 쪽을 택한다. 동성 4인(남복/여복)은 3가지 분할 모두 유효하다.

**ageCost** — 조합 내 최대·최소 출생년도 차이 / 10. `W_AGE = 0` (기본 비활성. "연령대 고려" 옵션 ON 시 3)

**genderTerm** — `oddPenalty`(3:1 감점, 항상 적용) + `policyBonus`(성별 정책). §5.5 참조

4. 최고점 조합을 슬롯에 배치하고, 채택된 2:2 분할을 팀A/팀B로 기록하며, 결정된 `matchType`을 슬롯에 함께 저장한다.

### 5.4 부분 충원과 풀 최소 확보

- 이미 있는 인원은 **고정**하고, 남은 자리 수만큼만 후보군에서 조합 탐색
- 예: Q5에 2명 있음 → 후보 10명 중 2명 조합 C(10,2)=45개 평가
- 이 경우에도 `repeatPenalty`/`balanceCost`는 **완성된 4인 기준**으로 계산

#### 풀 최소 확보 인원 (`minPool`, 기본 4)

**대기 슬롯 7개를 끝까지 채우면 안 된다.** 출석 20~24명 규모에서는 코트 12명 + 슬롯 28명 자리가 인원보다 많아, 자동 충원이 풀을 완전히 비운다. 풀이 비면 경기를 마친 4명이 **선택의 여지 없이** 그대로 다음 슬롯에 다시 묶이고, 중복 회피 로직이 무력해진다.

```js
const ready = queues.filter(q => q.members.length === 4).length;
if (ready >= 1 && pool.length - need < minPool) break;   // 더 채우지 않는다
```

- 완성된 대기 팀이 **최소 1개**는 항상 있어야 코트가 놀지 않는다. 그 조건을 만족한 뒤부터는 풀에 `minPool`명을 남긴다.
- 구현본 실측: 이 규칙 적용 전 직전 4인 재결합률 100%, 적용 후 **0%** (부록 E).
- 출석이 16명 이하로 적으면 수학적으로 풀을 남길 수 없다. 이때는 재결합이 발생하며, 정상이다.

### 5.5 성별 처리 (3:1 감점 + 성별 정책)

성별 관련 점수는 **성격이 다른 두 항목**으로 분리한다. 하나는 항상 켜져 있고, 하나는 설정으로 켠다.

```
genderTerm(combo) = oddPenalty(combo)      // ① 항상 적용
                  + policyBonus(combo)     // ② genderPolicy에 따름
```

#### ① 3:1 감점 (oddPenalty) — 항상 적용

남3여1 / 남1여3 구성은 실전에서 가장 불편한 조합이므로 정책과 무관하게 강하게 억제한다.

```js
const W_ODD = -250;          // 게임 2.5개 값어치. 사실상 차단 수준
const ODD_RELAX_THRESHOLD = 2;

function oddPenalty(combo, maxGames) {
  const m = combo.filter(isM).length;
  if (m !== 3 && m !== 1) return 0;          // 3:1 아님

  // 공정성 예외: 조합 안에 2게임 이상 뒤처진 사람이 있으면 감점을 면제한다
  const behind = maxGames - Math.min(...combo.map(p => p.gameCount));
  if (behind >= ODD_RELAX_THRESHOLD) return 0;

  return W_ODD;
}
```

**공정성 예외가 반드시 필요한 이유** — 여성이 1명만 출석한 날, 감점만 걸어두면 그 사람은 어떤 조합에도 들어갈 수 없어 영구히 대기한다. 시뮬레이션에서 이 예외가 없을 때 해당 인원의 게임 수가 3.1게임(남성 평균 5.8)으로 주저앉았고, 예외를 넣자 4.8게임으로 회복됐다(부록 D). **성별 구성은 공정성을 이길 수 없다**는 원칙을 코드로 강제하는 장치다.

#### ② 성별 정책 (policyBonus)

| 값 | 이름 | 남4/여4 | 남2여2 | 동작 |
|---|---|:--:|:--:|---|
| `FREE` | 성별 무시 **(기본값)** | 0 | 0 | 조합 유도 없음. 결과 구성대로 유형만 표기 |
| `PREFER_SAME` | 동성 우선 | +60 | 0 | 남복·여복을 선호 |
| `PREFER_MIXED` | 혼복 우선 | 0 | +20 | 혼복을 선호 |
| `STRICT_SAME` | 동성 강제 | 허용 | 배제 | 남4·여4만 허용 |
| `STRICT_MIXED` | 혼복 강제 | 배제 | 허용 | 남2여2만 허용 |

```js
function policyBonus(combo, policy) {
  const m = combo.filter(isM).length, f = 4 - m;
  const same = (m === 4 || f === 4), mixed = (m === 2 && f === 2);
  switch (policy) {
    case 'FREE':         return 0;
    case 'PREFER_SAME':  return same  ? 60 : 0;
    case 'PREFER_MIXED': return mixed ? 20 : 0;
    case 'STRICT_SAME':  return same  ? 0 : -Infinity;   // 조합 자체를 배제
    case 'STRICT_MIXED': return mixed ? 0 : -Infinity;
  }
}
```

**기본값이 `FREE`인 이유** — 이 클럽의 성비(남 29 / 여 12)에서 시뮬레이션한 결과다(부록 D).

- `PREFER_SAME`은 여성을 4명 단위로 묶어 쓰기 때문에 남는 여성이 3:1 조합으로 밀려난다. 그 결과 **혼복이 0%로 사라지면서 혼성(3:1)은 16%나 남는** 최악의 조합이 나왔다.
- `STRICT_MIXED`는 여성 게임 수 8.1 / 남성 3.3으로 공정성이 완전히 무너졌다.
- `FREE`는 게임 수 편차를 1 안팎으로 유지하면서 남복 50%대 · 혼복 25~42% · 여복 소량이라는, 실제 클럽 운영과 가장 비슷한 분포를 만들었다.

`PREFER_MIXED`의 가산점을 60이 아닌 **20**으로 낮춘 이유도 같다. 60을 주면 혼복이 59%까지 올라가면서 여복이 0%가 되고 남성 게임 수가 여성보다 적어진다. 20이면 혼복을 늘리면서도 균형이 유지된다.

**후보군 확장 규칙** — `STRICT_*` 정책에서는 상위 K명(기본 10) 안에서 유효 조합을 못 찾을 수 있다. 이때 K를 20까지 단계적으로 늘려 재탐색하고, 그래도 실패하면 슬롯을 미완성으로 두고 "여성 2명 부족" 같은 안내를 표시한다.

**UNKNOWN 처리** — 성별 미입력자가 섞인 조합은 `oddPenalty`·`policyBonus` 모두 0으로 계산하고, 유형은 `UNKNOWN`으로 표기한다.

### 5.6 경기 유형 판정 규칙

4인과 2:2 분할이 정해지면 다음 순서로 유형을 결정한다.

```js
function resolveMatchType(members, teams) {
  const g = members.map(p => p.gender);
  if (g.some(x => x !== 'M' && x !== 'F')) return 'UNKNOWN';

  const m = g.filter(x => x === 'M').length;
  const f = g.filter(x => x === 'F').length;

  if (m === 4) return 'MD';              // 남복
  if (f === 4) return 'WD';              // 여복

  if (m === 2 && f === 2 && teams) {
    const mixedA = teams.A.some(isM) && teams.A.some(isF);
    const mixedB = teams.B.some(isM) && teams.B.some(isF);
    return (mixedA && mixedB) ? 'XD' : 'MX';   // 혼복 : 혼성(남남 vs 여여)
  }
  return 'MX';                           // 3:1 등 나머지 혼재
}
```

| 코드 | 표기 | 배지색 | 조건 |
|---|---|---|---|
| `MD` | 남복 | 파랑 `#2563eb` | 남 4 |
| `WD` | 여복 | 자주 `#db2777` | 여 4 |
| `XD` | 혼복 | 보라 `#7c3aed` | 남2·여2이고 각 팀이 남1+여1 |
| `MX` | 혼성 | 회색 `#64748b` | 성별 혼재이나 위 조건 미충족 (남3여1, 남1여3, 남2여2의 동성팀 대결) |
| `UNKNOWN` | 미정 | 연회색 | 성별 미입력자 포함 |

**수동 선택 시 허용 유형** — 배지를 터치하면 현재 4인 구성으로 **가능한 유형만** 활성화된다.

| 성별 구성 | 선택 가능 |
|---|---|
| 남4 | 남복만 |
| 여4 | 여복만 |
| 남2·여2 | **혼복** / **혼성**(남남 vs 여여) 중 선택 |
| 남3여1 / 남1여3 | 혼성만 |

남2·여2에서 유형을 바꾸면 팀 분할이 자동으로 재구성된다. 혼복 선택 시 남1여1 분할 2가지 중 급수 균형이 나은 쪽, 혼성 선택 시 남남 vs 여여 분할로 고정한다. 수동 선택된 슬롯/코트는 잠금 처리되어 자동 로직이 유형을 되돌리지 않는다.

### 5.7 자동화 트리거와 실행 순서

**실행 순서는 반드시 다음과 같다.** 순서가 틀리면 대기열이 FIFO로 동작하지 않는다.

```
① pushToCourt()    빈 코트에 4인 완성된 앞 슬롯 투입
② compactQueues()  뒤 슬롯을 앞으로 당김
③ fillQueues()     남은 빈자리를 풀에서 충원
   → 변경이 없을 때까지 반복 (최대 12회)
```

> **충원(③)이 당기기(②)보다 먼저 실행되면 심각한 결함이 생긴다.** Q1이 코트로 빠져나가 비는 순간 방금 경기를 마친 인원으로 Q1이 다시 채워지고, Q2 이하의 팀은 영원히 앞으로 오지 못한다. 구현 초기에 이 순서로 만들었더니 출석 20명 중 4명이 2시간 동안 **한 게임도 치지 못했다**(게임 수 편차 7.8). 순서를 고친 뒤 편차 1.0으로 정상화됐다.

자동 모드 ON 상태에서 아래 이벤트 발생 시 재계산:

| 트리거 | 동작 |
|---|---|
| 출석 체크 | 풀 추가 → 빈 슬롯 자리 충원 |
| 경기 종료 | 4인 풀 복귀 → 코트 비면 Q1 투입 → 슬롯 당김 → 맨 뒤 충원 |
| 코트가 빔 | Q1이 4인 완성이면 즉시 투입 (설정: 즉시 / 확인 후) |
| 슬롯 비워짐 | 뒤 슬롯 당김 + 맨 뒤 충원 |
| 수동 [자동 정렬] | 잠기지 않은 슬롯 전체 재구성 |

### 5.8 자동화 금지 규칙

- `locked = true`인 슬롯/코트는 **읽기만 하고 절대 수정하지 않는다**
- 경기 중(`status = PLAYING`)인 코트는 어떤 경우에도 자동 변경 대상이 아니다
- 자동 배치로 인원이 바뀐 슬롯은 2초간 하이라이트 애니메이션 → 사람이 변경을 인지할 수 있게

### 5.9 의사코드

```js
function autoFillQueues(state) {
  if (!state.settings.autoMode) return state;

  // 1. 앞으로 당기기
  compactQueues(state);          // 잠긴 슬롯은 자리 유지

  // 2. 빈자리 채우기 (Q1부터)
  for (const slot of state.queues) {
    if (slot.locked) continue;
    const need = 4 - slot.members.length;
    if (need <= 0) continue;

    const pool = getPoolMembers(state);
    if (pool.length < need) break;          // 인원 부족 시 중단

    // 슬롯에 유형 핀(F-409)이 걸려 있으면 해당 성별만 후보로
    let eligible = filterByPinnedType(pool, slot);

    const candidates = eligible
      .sort((a, b) => priority(b) - priority(a))
      .slice(0, CANDIDATE_K);

    let best = bestCombination(candidates, need, slot.members, state);

    // STRICT 정책에서 유효 조합이 없으면 후보군을 넓혀 재시도
    if (!best && isStrict(state.settings.genderPolicy)) {
      const wider = eligible.sort((a, b) => priority(b) - priority(a)).slice(0, 20);
      best = bestCombination(wider, need, slot.members, state);
    }
    if (!best) { slot.notice = buildShortageNotice(slot, state); continue; }

    slot.members   = [...slot.members, ...best.picked];
    slot.teamSplit = best.split;            // { A:[id,id], B:[id,id] }
    slot.matchType = best.matchType;        // 'MD' | 'WD' | 'XD' | 'MX' | 'UNKNOWN'
    slot.notice    = null;
    markHighlight(slot);
  }
  return state;
}

function bestCombination(candidates, need, fixed, state) {
  const policy = state.settings.genderPolicy;
  let best = null;

  for (const combo of kCombinations(candidates, need)) {
    const full = [...fixed, ...combo];

    if (full.length < 4) {                  // 4인 미만이면 밸런스·유형 평가 생략
      const s = sum(combo.map(priority));
      if (!best || s > best.score) best = { score: s, picked: combo, split: null, matchType: null };
      continue;
    }

    const gt = oddPenalty(full, maxGames) + policyBonus(full, policy);
    if (gt === -Infinity) continue;         // STRICT 정책 위반 조합은 배제

    // 목표 유형에 맞는 분할만 후보로 두고, 그중 급수 균형이 최적인 것을 선택
    const split = bestSplit(full, targetTypeOf(full, policy, slot));
    if (!split) continue;

    const s = sum(combo.map(priority))
            - W_REPEAT * repeatPenalty(full, state)
            - W_BAL    * split.cost
            - W_AGE    * ageCost(full)
            + gt;

    if (!best || s > best.score) {
      best = {
        score: s,
        picked: combo,
        split: split.teams,
        matchType: resolveMatchType(full, split.teams)
      };
    }
  }
  return best;
}

// 3가지 2:2 분할 중 목표 유형 제약을 만족하는 것만 평가
function bestSplit(four, targetType) {
  const splits = [
    { A: [four[0], four[1]], B: [four[2], four[3]] },
    { A: [four[0], four[2]], B: [four[1], four[3]] },
    { A: [four[0], four[3]], B: [four[1], four[2]] },
  ];
  const valid = splits.filter(sp => {
    const t = resolveMatchType(four, sp);
    if (targetType === 'XD') return t === 'XD';   // 혼복은 남1여1 팀만
    if (targetType === 'MX') return t === 'MX';
    return true;                                   // 동성 4인은 모든 분할 유효
  });
  if (valid.length === 0) return null;

  return valid
    .map(sp => ({ teams: sp, cost: Math.abs(gradeSum(sp.A) - gradeSum(sp.B)) }))
    .sort((a, b) => a.cost - b.cost)[0];
}

// 목표 유형: 슬롯 핀 > 성별 정책 > 구성상 기본값
function targetTypeOf(four, policy, slot) {
  if (slot?.pinnedType) return slot.pinnedType;
  const m = four.filter(isM).length, f = four.filter(isF).length;
  if (m === 4) return 'MD';
  if (f === 4) return 'WD';
  if (m === 2 && f === 2) return 'XD';   // 남2여2는 기본 혼복
  return 'MX';
}
```

---

## 6. 상태 모델

### 6.1 인원 상태 전이

```
        출석 체크
  [미출석] ──────► [POOL] ◄──────────────┐
      ▲               │                  │
      │        자동/수동 배정             │ 경기 종료(기본)
      │               ▼                  │
   출석 해제      [QUEUED]                │
      │               │                  │
      │        코트 투입(Q1 or 수동)      │
      │               ▼                  │
      └────────── [PLAYING] ─────────────┘
                      │
                      │ 경기 종료 + 리벤지
                      ▼
                  [QUEUED] (팀 유지, 맨 뒤 슬롯)
```

### 6.2 코트 상태 전이

```
[EMPTY] ──(1~3명 배치)──► [FILLING] ──(4명 완성 + 시작)──► [PLAYING]
   ▲                          │                              │
   │                          │ 인원 제거                     │ 종료
   └──────────────────────────┘                              │
   └─────────────────────────────────────────────────────────┘

[DISABLED]  ← 설정에서 "사용 안 함" 지정 (자동 투입 제외)
```

---

## 7. 데이터 모델

### 7.1 ERD 개요

```
members ──< session_attendees >── sessions
                  │
                  └──< match_players >── matches ──> sessions
```

### 7.2 스키마 (SQL 표기 / IndexedDB도 동일 구조)

```sql
-- 회원 마스터 (영구)
CREATE TABLE members (
  id            TEXT PRIMARY KEY,          -- uuid
  name          TEXT NOT NULL,
  birth_year    INTEGER,                   -- 1975
  grade         TEXT NOT NULL,             -- 'S','A','B','C','D','E'
  gender        TEXT NOT NULL              -- 'M' | 'F' (필수)
                CHECK (gender IN ('M','F')),
  phone         TEXT,
  is_active     INTEGER DEFAULT 1,
  memo          TEXT,
  created_at    TEXT,
  updated_at    TEXT
);
CREATE INDEX idx_members_active ON members(is_active, name);
CREATE INDEX idx_members_gender ON members(gender, grade);

-- 급수 정의 (편집 가능)
CREATE TABLE grades (
  code    TEXT PRIMARY KEY,   -- 'S'
  label   TEXT NOT NULL,      -- '자강'
  weight  INTEGER NOT NULL,   -- 6
  color   TEXT,               -- '#c2410c'
  sort    INTEGER
);

-- 운동일 세션
CREATE TABLE sessions (
  id          TEXT PRIMARY KEY,
  date        TEXT NOT NULL UNIQUE,   -- '2026-08-10'
  started_at  TEXT,
  closed_at   TEXT,
  court_count INTEGER DEFAULT 3,
  status      TEXT DEFAULT 'OPEN'     -- OPEN | CLOSED
);

-- 당일 출석자 + 세션 상태
CREATE TABLE session_attendees (
  id             TEXT PRIMARY KEY,
  session_id     TEXT NOT NULL,
  member_id      TEXT,                -- 게스트면 NULL
  guest_name     TEXT,                -- 게스트 전용
  guest_gender   TEXT,                -- 게스트 전용 'M'|'F' (게스트일 때 필수)
  guest_grade    TEXT,                -- 게스트 전용, 미입력 시 'C' 기본
  checked_in_at  TEXT NOT NULL,
  checked_out_at TEXT,
  game_count     INTEGER DEFAULT 0,
  last_game_end  TEXT,                -- 대기시간 계산 기준
  state          TEXT DEFAULT 'POOL', -- POOL | QUEUED | PLAYING
  FOREIGN KEY(session_id) REFERENCES sessions(id),
  FOREIGN KEY(member_id)  REFERENCES members(id)
);
CREATE UNIQUE INDEX idx_att ON session_attendees(session_id, member_id);

-- 경기 기록
CREATE TABLE matches (
  id          TEXT PRIMARY KEY,
  session_id  TEXT NOT NULL,
  court_no    INTEGER NOT NULL,
  started_at  TEXT NOT NULL,
  ended_at    TEXT,
  match_type  TEXT NOT NULL           -- MD | WD | XD | MX | UNKNOWN
              CHECK (match_type IN ('MD','WD','XD','MX','UNKNOWN')),
  type_source TEXT DEFAULT 'AUTO',    -- AUTO | MANUAL (수동 변경 여부)
  status      TEXT DEFAULT 'PLAYING'  -- PLAYING | DONE | CANCELLED
);
CREATE INDEX idx_matches_type ON matches(session_id, match_type);

CREATE TABLE match_players (
  match_id    TEXT NOT NULL,
  attendee_id TEXT NOT NULL,
  team        TEXT NOT NULL,          -- 'A' | 'B'
  PRIMARY KEY (match_id, attendee_id)
);

-- 화면 배치 상태 (앱 재시작/새로고침 복원용)
CREATE TABLE board_state (
  session_id  TEXT PRIMARY KEY,
  payload     TEXT NOT NULL,          -- JSON: courts[], queues[], settings
  updated_at  TEXT
);
```

### 7.3 런타임 상태 JSON

```jsonc
{
  "sessionId": "s-20260810",
  "settings": {
    "autoMode": true,
    "autoStartOnFull": false,       // 4명 차면 자동 시작할지
    "autoPushToCourt": true,        // 코트 비면 Q1 자동 투입
    "courtCount": 3,
    "queueSlotCount": 7,
    "matchWarnMinutes": 18,
    "considerAge": false,
    "genderPolicy": "FREE",         // FREE | PREFER_SAME | PREFER_MIXED | STRICT_SAME | STRICT_MIXED
    "showMatchTypeBadge": true,
    "weights": { "game": 100, "wait": 3, "new": 40,
                 "repeat": 25, "balance": 10, "age": 0,
                 "odd": -250, "same": 60, "mixed": 20 },
    "oddRelaxThreshold": 2,         // N게임 이상 뒤처지면 3:1 감점 면제
    "repeatLookback": 3,
    "candidateK": 10,
    "minPool": 4
  },
  "courts": [
    {
      "no": 1,
      "status": "PLAYING",           // EMPTY | FILLING | PLAYING | DISABLED
      "locked": false,
      "matchId": "m-0012",
      "startedAt": "2026-08-10T19:22:10+09:00",
      "teams": { "A": ["a-101", "a-118"], "B": ["a-107", "a-133"] },
      "matchType": "XD",             // MD | WD | XD | MX | UNKNOWN
      "typeSource": "AUTO"           // AUTO | MANUAL
    }
  ],
  "queues": [
    { "index": 1, "locked": false,
      "members": ["a-120", "a-104", "a-115", "a-109"],
      "teams": { "A": ["a-120","a-115"], "B": ["a-104","a-109"] },
      "matchType": "MD",
      "typeSource": "AUTO",
      "pinnedType": null,            // 유형 핀(F-409). 지정 시 해당 성별만 자동 충원
      "notice": null,                // 예: "여성 1명 부족"
      "origin": "AUTO" },            // AUTO | MANUAL | REVENGE
    { "index": 2, "locked": true, "members": ["a-131","a-142"],
      "pinnedType": "WD", "matchType": null, "origin": "MANUAL" }
  ],
  "pool": ["a-102", "a-111", "a-125"],
  "undoStack": [ /* 최근 20개 액션 */ ]
}
```

---

## 8. 화면 설계

### 8.1 화면 구성

| 화면 | 경로 | 용도 |
|---|---|---|
| 운영 대시보드 | `/` | 메인. 코트 + 대기열 + 풀 |
| 출석 체크 | `/checkin` | 회원 그리드, 터치 토글 |
| 회원 관리 | `/members` | 등록/수정/CSV/백업 (PIN 필요) |
| 설정 | `/settings` | 코트 수, 가중치, 자동화 옵션 |
| 기록 | `/history` | 세션별 요약, 개인별 게임 수 |

### 8.2 운영 대시보드 레이아웃 (태블릿 가로 1280×800 기준)

```
┌──────────────────────────────────────────────────────────────────────┐
│ 8/10(월) · 출석 23명(남15/여8) · 진행 3   [자동 ●] [정렬] [↩] [≡]   │ 56px
├────────────────────────────────────────┬─────────────────────────────┤
│  코트 1  〔혼복〕  ⏱ 07:42   [종료]     │  대기열                     │
│  ┌──────────┬──────────┐               │ ┌─────────────────────────┐ │
│  │♂김철수 B │♂이민수 B │  A팀          │ │ Q1 〔남복〕 🔓          │ │
│  │♀박영희 C │♀최수진 C │  B팀          │ │ ♂정다은B ♂한지훈B       │ │
│  └──────────┴──────────┘               │ │ ♂오세영C ♂남기웅C [투입]│ │
│                                        │ └─────────────────────────┘ │
│  코트 2  〔남복〕  ⏱ 12:05 ⚠ [종료]     │ ┌─────────────────────────┐ │
│  ┌──────────┬──────────┐               │ │ Q2 〔여복📌〕 🔒 (수동)  │ │
│  │ ...      │ ...      │               │ │ ♀문상호A ♀배준영A       │ │
│  └──────────┴──────────┘               │ │ ＋빈자리  ＋빈자리      │ │
│                                        │ │ ⚠ 여성 2명 부족          │ │
│  코트 3  〔여복〕  대기중   [시작]       │ └─────────────────────────┘ │
│  ┌──────────┬──────────┐               │ ┌─────────────────────────┐ │
│  │♀강태원 A │ ＋       │               │ │ Q3 … Q7 (스크롤)        │ │
│  └──────────┴──────────┘               │ └─────────────────────────┘ │
├────────────────────────────────────────┴─────────────────────────────┤
│ 대기 인원(풀) 8명   [게임수순 ▾] [전체|♂|♀]                          │
│ (♂윤서준 B·0G·12분) (♀조민혁 C·1G·8분) (♂신아름 D·1G·5분) …        │ 120px
└──────────────────────────────────────────────────────────────────────┘
```

**영역 비율** — 코트 영역 55% / 대기열 영역 45% (좌우), 하단 풀 고정 120px

**경기 유형 배지** — 코트·슬롯 헤더의 `〔 〕` 부분. 4명이 차면 자동 표시되고, 터치하면 유형 선택 팝업(§8.7)이 열린다. 📌는 유형 핀(F-409)이 걸린 슬롯 표시.

### 8.3 인원 칩(Chip) 사양

```
┌────────────────────────┐
│ ● ♂ 김철수      2G     │   ● 급수 색상 도트, ♂/♀ 성별 아이콘
│     '82 · B    12분    │   2G = 당일 게임 수, 12분 = 대기 시간
└────────────────────────┘
```

- 최소 크기 **160×72px** (장갑/땀 손 대응)
- 급수별 좌측 컬러 바 + 도트 (색맹 대비: 급수 텍스트 병기)
- **성별 표기**: 이름 앞 ♂/♀ 아이콘 + 칩 배경에 아주 옅은 색조(남 `#1e3a5f` 8%, 여 `#5f1e4a` 8%). 급수 색과 충돌하지 않도록 채도를 낮게 유지
- 성별 미입력(`UNKNOWN`)은 `?` 아이콘 + 점선 테두리로 표시해 보정을 유도
- 게임 수 0 → 배지 강조(빨강), 대기 10분 초과 → 시간 텍스트 강조
- 드래그 중: 8도 기울기 + 그림자, 드롭 가능 영역은 점선 테두리 발광

### 8.4 게임 종료 다이얼로그

```
┌─────────────────────────────────────┐
│  코트 1 경기 종료 〔혼복〕 (07:42)   │
│                                     │
│  ♂김철수·♀박영희 vs ♂이민수·♀최수진│
│                                     │
│  [ 전원 대기 인원으로 ]  ← 기본     │
│  [ 리벤지 — 4명 그대로 대기팀 ]     │
│  [ 팀 나눠서 유지 ]                 │
│  [ 취소 ]                           │
└─────────────────────────────────────┘
```

- 5초 카운트다운 후 기본 선택 자동 실행 (설정으로 끌 수 있음)
- 리벤지 선택 시 → 빈 대기 슬롯 중 가장 뒤에 4인 팀째로 삽입, `origin=REVENGE`, 자동 잠금

### 8.5 출석 체크 화면

```
┌──────────────────────────────────────────────────────┐
│ 출석 체크   [ㄱㄴㄷ|급수|최근] [전체|♂|♀] [🔍 초성]   │
├──────────────────────────────────────────────────────┤
│ ┌────────┐ ┌────────┐ ┌────────┐ ┌────────┐         │
│ │♀강태원 │ │♂김철수✓│ │♀김하늘 │ │♂남기웅✓│  ← 5열  │
│ │ A '79  │ │ B '82  │ │ C '95  │ │ C '88  │         │
│ └────────┘ └────────┘ └────────┘ └────────┘         │
│  (회색)     (초록)      (회색)     (초록)             │
├──────────────────────────────────────────────────────┤
│ 출석 23 (♂15 / ♀8)      [게스트 추가]  [운영 화면 →] │
└──────────────────────────────────────────────────────┘
```

- 카드 크기 200×110px, 5열 그리드, 터치 시 즉시 색 전환 + 짧은 진동(Vibration API)
- 출석 완료자는 상단으로 모으지 **않는다** (자리 기억 유지 → 오터치 감소)
- 상단 성별 필터로 남/여만 보기 가능 (여성 회원이 소수일 때 찾기 쉬움)
- 하단에 남/여 출석 수를 항상 표시 → 오늘 어떤 유형 경기를 몇 개 돌릴 수 있는지 즉시 판단

**게스트 추가 다이얼로그**

```
┌──────────────────────────────┐
│  게스트 추가                 │
│  이름  [____________]        │
│  성별  ( ♂ 남 ) ( ♀ 여 ) ←필수│
│  급수  [ C ▾ ]  (기본 C)     │
│  [ 추가 ]        [ 취소 ]    │
└──────────────────────────────┘
```

### 8.6 경기 유형 배지 및 선택 팝업

**배지 표시 규칙**

| 상태 | 표시 |
|---|---|
| 4명 미만 | 배지 없음 (또는 핀 지정 시 `〔여복📌〕` 회색 점선) |
| 4명 완성 | 판정된 유형 배지 표시 |
| 수동 변경됨 | 배지 우측에 작은 `✎` 표시 |
| 성별 미입력 포함 | `〔미정〕` 연회색 |

**선택 팝업 (배지 터치 시)**

```
┌──────────────────────────────────────┐
│  코트 3 경기 유형                    │
│  ♂강태원  ♀신아름  ♂조민혁  ♀윤서준  │
│                                      │
│  ○ 남복      (불가 — 여성 2명 포함)  │  ← 비활성, 사유 표시
│  ○ 여복      (불가 — 남성 2명 포함)  │
│  ● 혼복   ♂강태원·♀신아름 vs ♂조민혁·♀윤서준
│  ○ 혼성   ♂강태원·♂조민혁 vs ♀신아름·♀윤서준
│                                      │
│           [ 적용 ]      [ 취소 ]     │
└──────────────────────────────────────┘
```

- 선택 불가 유형은 **비활성 + 사유 문구**를 함께 보여 준다. 감추지 않는 이유는 왜 못 고르는지 바로 이해되게 하기 위함이다.
- 각 선택지 옆에 해당 유형일 때의 **실제 팀 편성을 미리보기**로 노출한다.
- [적용] 시 팀 분할이 재구성되고 `typeSource = MANUAL`, 슬롯/코트 잠금
- 이미 시작된 경기(`PLAYING`)에서도 유형 변경은 가능하나, 팀 분할은 바뀌지 않고 표기만 바뀐다 (경기 중 코트를 바꾸지 않는다는 원칙 유지)

### 8.7 UI 원칙

| 항목 | 기준 |
|---|---|
| 최소 터치 타깃 | 72×72px (칩 168×72, 버튼 88×88) |
| 기본 폰트 크기 | 본문 18px, 이름 22px Bold, 타이머 32px |
| 명암비 | 체육관 조명 대응 WCAG AA 이상. 다크 테마 기본 |
| 화면 꺼짐 방지 | Wake Lock API 사용 |
| 파괴적 동작 | 확인 다이얼로그 + Undo 제공 (경기 종료, 출석 해제, 세션 마감) |
| 피드백 | 모든 배치 변경 시 토스트 + 해당 카드 2초 하이라이트 |
| 성별·유형 표기 | 색상만으로 구분하지 않는다. 성별은 ♂/♀ 기호, 유형은 "남복/여복/혼복/혼성" 텍스트를 항상 함께 표기 |

---

## 9. 기술 아키텍처

### 9.1 배포 방식 2안

**A안 — 단독 실행 (v1 권장 시작점)**

```
단일 HTML 파일 (또는 정적 번들)
  ├─ React 19 (또는 Preact) — CDN 또는 번들
  ├─ dnd-kit (터치 지원 드래그앤드롭)
  ├─ Dexie.js → IndexedDB (로컬 영구 저장)
  └─ PWA (Service Worker) → 오프라인 완전 동작
```

- 장점: 인터넷 불필요, 서버 비용 0, 체육관 와이파이 불안정에 무관
- 단점: 기기 1대에서만 상태 유지. 다른 태블릿과 공유 불가
- 백업: JSON export를 USB/구글드라이브에 주기 저장

**B안 — 다기기 실시간 동기화 (v2)**

```
정적 호스팅 (Vercel / Netlify / GitHub Pages)
  └─ Supabase (PostgreSQL + Realtime + Auth)
       └─ Realtime 구독으로 태블릿 N대 상태 동기화
```

- 시나리오: 출석용 태블릿 1대 + 대진판용 대형 화면 1대 분리 운영
- 장점: 회원 스마트폰에서 대기 순번 확인 가능(읽기 전용 뷰)
- 단점: 네트워크 의존. 오프라인 폴백 로직 필수

### 9.2 저장소 추상화 (필수 설계)

A안 → B안 전환 비용을 없애기 위해 저장소를 인터페이스로 격리한다.

```ts
interface StorageAdapter {
  // 회원
  listMembers(): Promise<Member[]>;
  upsertMember(m: Member): Promise<Member>;
  deactivateMember(id: string): Promise<void>;

  // 세션
  getOrCreateSession(date: string): Promise<Session>;
  closeSession(id: string): Promise<void>;

  // 출석
  listAttendees(sessionId: string): Promise<Attendee[]>;
  checkIn(sessionId: string, memberId: string): Promise<Attendee>;
  checkOut(attendeeId: string): Promise<void>;

  // 경기
  startMatch(m: MatchInput): Promise<Match>;
  endMatch(matchId: string): Promise<Match>;
  listMatches(sessionId: string): Promise<Match[]>;

  // 보드 상태
  loadBoard(sessionId: string): Promise<BoardState | null>;
  saveBoard(sessionId: string, s: BoardState): Promise<void>;

  // 실시간 (B안에서만 구현, A안은 no-op)
  subscribe(sessionId: string, cb: (s: BoardState) => void): () => void;
}

class IndexedDbAdapter implements StorageAdapter { /* A안 */ }
class SupabaseAdapter  implements StorageAdapter { /* B안 */ }
```

### 9.3 상태 관리

- 단일 스토어(Zustand 또는 useReducer) + **순수 함수 리듀서**
- 모든 배치 로직은 `(state, action) => newState` 순수 함수로 작성 → 단위 테스트 용이
- 액션 수행 시마다 `undoStack.push(prevState)` (최대 20)
- 상태 변경 후 300ms 디바운스로 `saveBoard()` 호출

**핵심 액션 목록**

```
CHECK_IN, CHECK_OUT, ADD_GUEST
MOVE_PLAYER      { playerId, from, to }        // from/to = pool|queue:N:slot|court:N:slot
SWAP_PLAYER      { aId, bId }
MOVE_QUEUE_SLOT  { from, to }
TOGGLE_LOCK      { target }
FILL_QUEUES                                    // 자동 충원
PUSH_TO_COURT    { queueIndex, courtNo }
START_MATCH      { courtNo }
END_MATCH        { courtNo, disposition }      // POOL | REVENGE | PARTIAL
REBALANCE_TEAMS  { courtNo | queueIndex }
SET_MATCH_TYPE   { target, matchType }         // 수동 유형 변경 + 팀 재구성
PIN_QUEUE_TYPE   { queueIndex, matchType|null } // 슬롯 유형 핀 지정/해제
CYCLE_TEAM_SPLIT { target }                    // 2:2 분할 3가지 순환
SET_GENDER_POLICY { policy }
UNDO, REDO
```

### 9.4 드래그앤드롭 구현 주의

- HTML5 Drag&Drop API는 터치 기기에서 신뢰할 수 없음 → **Pointer Events 기반 라이브러리 필수**
- 권장: `@dnd-kit/core` (터치 센서 + 접근성 + 키보드 지원)
- `touch-action: none`을 드래그 핸들에 지정, 스크롤 충돌 방지
- 드래그 시작 지연 150ms + 이동 임계값 8px → 스크롤과 드래그 오인 방지
- **반드시 탭-투-무브 대체 경로 제공** (F-502) — 드래그 실패 시 운영 중단 방지

### 9.5 B안 API 명세 (참고)

| 메서드 | 경로 | 설명 |
|---|---|---|
| GET | `/api/members` | 활성 회원 목록 |
| POST | `/api/members` | 회원 등록 |
| PATCH | `/api/members/:id` | 회원 수정 |
| GET | `/api/sessions/today` | 오늘 세션 조회/생성 |
| POST | `/api/sessions/:id/attendees` | 출석 체크 |
| DELETE | `/api/attendees/:id` | 출석 해제 |
| GET | `/api/sessions/:id/board` | 보드 상태 조회 |
| PUT | `/api/sessions/:id/board` | 보드 상태 저장 (낙관적 잠금: `version`) |
| POST | `/api/matches` | 경기 시작 |
| PATCH | `/api/matches/:id/end` | 경기 종료 |
| WS | `realtime:session:{id}` | 보드 상태 변경 브로드캐스트 |

동시 편집 충돌은 `board_state.version` 정수 증가로 감지, 충돌 시 최신 상태 리로드 후 재적용.

---

## 10. 비기능 요구사항

| 항목 | 요구 수준 |
|---|---|
| 대상 기기 | 안드로이드 태블릿 10인치 이상, 크롬 최신. iPad Safari 호환 |
| 응답 시간 | 모든 터치 반응 100ms 이내, 자동 배치 계산 200ms 이내 |
| 오프라인 | A안 100% 오프라인. B안은 네트워크 끊김 시 로컬 캐시로 계속 조작, 복구 시 동기화 |
| 데이터 보존 | 브라우저 캐시 삭제 대비 주 1회 JSON 백업 안내 배너 |
| 화면 유지 | Wake Lock으로 운영 중 화면 꺼짐 방지, 실패 시 안내 |
| 접근성 | 키보드 조작 가능, 포커스 링 표시, 급수 색상에 텍스트 병기 |
| 보안 | 관리 화면 PIN 4자리. 개인정보는 이름/출생년도/연락처만 저장, 외부 전송 없음 |
| 국제화 | 한국어 단일 (v1) |

---

## 11. 개발 단계

| 단계 | 산출물 | 완료 기준 |
|---|---|---|
| **M1** 데이터 기반 | 회원 CRUD, CSV 일괄 등록, IndexedDB 스키마 | 50명 등록 후 재시작해도 유지 |
| **M2** 출석 | 출석 체크 화면, 세션 생성/마감 | 20명 출석 체크가 30초 내 완료 |
| **M3** 수동 보드 | 코트 3면 + 대기 7슬롯 + 드래그앤드롭 + Undo | 자석판으로 하던 모든 조작이 화면에서 가능 |
| **M4** 자동화 | 우선순위 알고리즘, 자동 충원, 자동 투입, 리벤지, **성별 정책·경기 유형 판정** | 개입 없이 2시간 운영이 자동 순환하고 유형이 정확히 표기됨 |
| **M5** 기록·설정 | 경기 이력, 세션 요약, 가중치 설정, 백업/복원 | 세션 종료 시 개인별 게임 수 표 출력 |
| **M6** 안정화 | PWA, Wake Lock, 오류 복구, 실기기 테스트 | 실제 운동일 3회 무중단 운영 |
| **M7** (선택) 동기화 | Supabase 어댑터, 실시간 구독, 읽기 전용 모바일 뷰 | 태블릿 2대 상태 일치 |

> M1~M4까지가 현행 자석판을 완전히 대체하는 최소 기능(MVP)이다.

---

## 12. 테스트 시나리오

### 12.1 기능 테스트

| ID | 시나리오 | 기대 결과 |
|---|---|---|
| T-01 | 출석 20명, 자동 모드 ON | Q1~Q5가 4명씩 자동 완성, 풀에 0명 |
| T-02 | 코트1 종료 → 전원 풀 복귀 | 4명 게임 수 +1, Q1이 코트1 투입, Q2~Q7 앞당김, Q7 신규 충원 |
| T-03 | 코트1 종료 → 리벤지 선택 | 4명이 팀 유지한 채 가장 뒤 빈 슬롯에 들어감, 해당 슬롯 잠김 |
| T-04 | Q3을 수동 편집 후 자동 정렬 실행 | Q3만 그대로 유지, 나머지 슬롯 재구성 |
| T-05 | 게임 수 0인 사람 3명 존재 | 다음 조합에 3명 모두 우선 포함 |
| T-06 | 직전 게임 4명이 풀에 함께 있음 | repeatPenalty로 다음 조합에서 서로 분리됨 |
| T-07 | 출석 7명(2게임 불가) | Q1만 4명 완성, Q2는 3명에서 대기, 코트는 1면만 가동 |
| T-08 | 경기 중인 사람 출석 해제 시도 | 확인 다이얼로그 → 승인 시 코트에서 제거되고 코트는 FILLING |
| T-09 | 이동 후 Undo 5회 | 5단계 이전 배치로 정확히 복원 |
| T-10 | 앱 새로고침 | 코트/대기/풀/타이머가 그대로 복원 |
| T-11 | 자동 모드 OFF | 어떤 이벤트에도 자동 배치가 일어나지 않음 |
| T-12 | 게스트 2명 추가 | 정상 배치되고 세션 종료 시 DB에 남지 않음 |
| T-13 | 남자 4명으로 슬롯 완성 | 배지가 **남복**으로 자동 표시 |
| T-14 | 여자 4명으로 슬롯 완성 | 배지가 **여복**으로 자동 표시 |
| T-15 | 남2·여2로 슬롯 완성 | 팀 분할이 남1여1 vs 남1여1로 구성되고 배지가 **혼복** |
| T-16 | 남2·여2에서 유형을 혼성으로 변경 | 팀이 남남 vs 여여로 재구성, 배지 **혼성**, 슬롯 잠김 |
| T-17 | 남3·여1 조합 강제 배치 | 배지가 **혼성**, 유형 팝업에서 남복/여복/혼복 모두 비활성 |
| T-18 | 정책 `PREFER_SAME`, 남10·여4 출석 | 동성 조합이 우선 구성되나 게임 수 격차가 있으면 공정성이 우선함 |
| T-19 | 정책 `STRICT_MIXED`, 남10·여2 출석 | 남2여2 슬롯 1개만 완성, 나머지 슬롯은 미완성 + "여성 부족" 안내 |
| T-20 | Q4에 여복 핀 지정 | 자동 충원이 여성만 채움. 여성 부족 시 미완성 유지하고 Q5로 진행 |
| T-21 | 성별 미입력 회원 포함 | 배지 **미정**, 칩에 `?` 표시, genderBonus 0으로 계산되어 배치는 정상 진행 |
| T-22 | [팀 바꾸기] 3회 연타 | 3가지 분할을 순환하며 각 분할의 유형 배지가 함께 갱신됨 |
| T-23 | 경기 종료 후 기록 확인 | `matches.match_type`에 확정 유형과 `type_source`가 저장됨 |

### 12.2 공정성 검증 (시뮬레이션)

- 출석 24명(남16·여8), 3코트, 게임당 12분, 2시간 운영을 시뮬레이션
- **합격 기준**: 개인별 게임 수 최대-최소 차이 ≤ 1
- **합격 기준**: 동일 4인 조합이 3게임 이내 재발생 0건
- **합격 기준**: 팀 급수 가중치 합 차이 평균 ≤ 1.5
- **합격 기준**: 3:1(남3여1·남1여3) 구성 경기 비율 ≤ 15%
- **합격 기준**: 여성 평균 게임 수가 남성 평균 대비 −1 이내
- **합격 기준**: 여성 1명만 출석한 극단 케이스에서도 해당 인원의 게임 수가 전체 평균 −1.5 이내

> 마지막 두 항목이 이 클럽에서 특히 중요하다. 여성이 29%인 구성에서는 성별 조건을 조금만 세게 걸어도 여성이 대기에 갇힌다. 부록 D에 실측 결과를 정리했다.

### 12.3 현장 테스트

| 항목 | 기준 |
|---|---|
| 터치 정확도 | 땀 묻은 손·조명 아래에서 오터치율 5% 미만 |
| 출석 소요 | 20명 출석 완료까지 30초 이내 |
| 운영자 개입 | 2시간 운영 중 수동 개입 5회 이하 |
| 무중단 | 2시간 동안 새로고침·복구 조작 0회 |

---

## 13. 열린 이슈 / 확인 필요 사항

| # | 항목 | 선택지 | 기본 가정 |
|---|---|---|---|
| 1 | 태블릿 대수 | 1대 공용 / 출석용+대진판 분리 | **1대 공용** (A안). 분리 필요 시 M7 |
| 2 | 성별 정책 기본값 | FREE / PREFER_SAME / PREFER_MIXED / STRICT_* | **FREE** (부록 D 시뮬레이션 근거). 혼복을 더 원하면 `PREFER_MIXED` |
| 2-1 | 여복 확보 방법 | 자동 유도 vs 수동 지정 | 성비상 여복은 자동으로 잘 안 나온다. **슬롯 유형 핀(F-409)** 으로 "오늘 여복 한 게임"을 수동 지정하는 방식 권장 |
| 3 | 급수 체계 | S/A/B/C/D/E vs 클럽 자체 체계 | 편집 가능하게 구현, 초기값만 제공 |
| 4 | 경기 시간 | 시간제(예 15분) vs 점수제(21점) | **점수제 가정**, 타이머는 참고용 표시만 |
| 5 | 게스트 처리 | 게임 수 카운트에 포함 여부 | **포함** (공정성 계산 동일 적용) |
| 6 | 코트 우선순위 | 특정 코트를 고수 배정할지 | 없음. 비는 코트 순서대로 |
| 7 | 연령 반영 | 출생년도를 매칭에 쓸지 표시만 할지 | **표시만** (`W_AGE=0`), 옵션으로 활성화 가능 |

> 7번 관련: 출생년도는 요구사항상 DB에 저장하되, 매칭 로직 반영은 기본 비활성으로 두었다. "실력이 비슷하면 연령대도 맞춰달라" 같은 요청이 나오면 설정에서 켜면 된다.

---

## 부록 A. CSV 일괄 등록 포맷

```csv
이름,출생년도,급수,성별
김철수,1982,B,M
박영희,1990,C,F
강태원,1979,A,남
신아름,1993,D,여
```

- 헤더 행 필수, UTF-8
- 급수는 `grades` 테이블의 `code` 또는 `label` 모두 허용
- **성별은 필수**. `M`/`F`/`남`/`여`/`m`/`f` 허용, 공백이면 해당 행을 오류로 표시하고 나머지만 등록
- 가져오기 결과 화면에 `성공 48건 / 오류 2건(성별 누락)`처럼 건수와 사유를 함께 표시
- 동명이인은 출생년도로 구분, 완전 중복 시 등록 거부 후 목록 표시

## 부록 B. 설정 기본값

```jsonc
{
  "courtCount": 3,
  "queueSlotCount": 7,
  "autoMode": true,
  "autoPushToCourt": true,
  "autoStartOnFull": false,
  "endDialogAutoConfirmSec": 5,
  "matchWarnMinutes": 18,
  "candidateK": 10,
  "repeatLookback": 3,
  "minPool": 4,
  "considerAge": false,
  "genderPolicy": "FREE",
  "showMatchTypeBadge": true,
  "weights": {
    "game": 100, "wait": 3, "new": 40,
    "repeat": 25, "balance": 10, "age": 0,
    "odd": -250, "same": 60, "mixed": 20
  },
  "oddRelaxThreshold": 2,
  "theme": "dark",
  "adminPin": "0000"
}
```

## 부록 C. 경기 유형 판정 빠른 참조

개발·검수 시 이 표 하나로 대조할 수 있게 정리한다.

| 남 | 여 | 팀 분할 | 유형 | 배지 | 자동 채택 분할 |
|:--:|:--:|---|---|---|---|
| 4 | 0 | 임의 | `MD` | 남복 | 급수 균형 최적 (3가지 모두 유효) |
| 0 | 4 | 임의 | `WD` | 여복 | 급수 균형 최적 (3가지 모두 유효) |
| 2 | 2 | 남1여1 vs 남1여1 | `XD` | 혼복 | **기본값.** 유효 분할 2가지 중 급수 균형 우수한 쪽 |
| 2 | 2 | 남남 vs 여여 | `MX` | 혼성 | 수동 선택 시에만 채택 |
| 3 | 1 | 임의 | `MX` | 혼성 | 급수 균형 최적. `PREFER_SAME`에서 감점 대상 |
| 1 | 3 | 임의 | `MX` | 혼성 | 위와 동일 |
| 성별 미입력 포함 | | 임의 | `UNKNOWN` | 미정 | 급수 균형 최적, genderBonus 0 |

**유형 결정 우선순위**

```
1) 사람이 수동 선택한 유형        (typeSource = MANUAL, 잠금)
2) 슬롯에 걸린 유형 핀            (pinnedType)
3) 성별 구성에 따른 자동 판정      (§5.6)
```

**정책별 조합 선호 요약** (3:1 감점 −250은 정책과 무관하게 항상 적용)

| 정책 | 남4 / 여4 | 남2여2 | 남3여1 / 남1여3 |
|---|:--:|:--:|:--:|
| `FREE` **(기본)** | 0 | 0 | −250 |
| `PREFER_SAME` | +60 | 0 | −250 |
| `PREFER_MIXED` | 0 | +20 | −250 |
| `STRICT_SAME` | 허용 | 배제 | 배제 |
| `STRICT_MIXED` | 배제 | 허용 | 배제 |

> **−250은 게임 2.5개 값어치**로, 3:1 조합을 사실상 차단한다. 다만 조합 안에 2게임 이상 뒤처진 사람이 있으면 감점이 면제되어, 소수 성별 인원이 대기에 갇히지 않는다. 반대로 가산점 60·20은 게임 1개 차이(100점)보다 작다. **성별 선호는 공정성을 이길 수 없고, 3:1 회피는 공정성에 진다** — 이 두 부등식이 수치 설계의 핵심이다.

## 부록 D. 실제 클럽 성비 기준 시뮬레이션 결과

**전제** — 회원 41명(남 29 · 여 12, 여성 29.3%), 3코트, 게임당 10~14분, 2시간 운영. 각 조건 25회 반복 평균. §5의 알고리즘을 그대로 구현해 측정.

### D.1 성별 정책 비교 (출석 24명 = 남17·여7)

| 정책 | 게임수 편차 | 남 평균 | 여 평균 | 남복 | 여복 | 혼복 | 혼성(3:1) |
|---|---:|---:|---:|---:|---:|---:|---:|
| **`FREE`** | **1.2** | 4.83 | 4.47 | 51% | 7% | 42% | **0%** |
| `PREFER_SAME` | 0.9 | 4.85 | 4.45 | 68% | 16% | **0%** | **16%** |
| `PREFER_MIXED` (+20) | 1.2 | 4.72 | 4.74 | 41% | 0% | 59% | 0% |
| `STRICT_SAME` | 1.1 | 4.84 | 4.43 | 74% | 26% | 0% | 0% |
| `STRICT_MIXED` | **5.3** | **3.33** | **8.10** | 0% | 0% | 100% | 0% |

**읽는 법**

- `PREFER_SAME`은 이 성비에서 **가장 나쁜 선택**이다. 여성을 4명 단위로 묶어 여복을 만들다 보니 남는 여성이 3:1로 밀려나, 정작 혼복은 0%가 되고 혼성만 16% 남는다.
- `STRICT_MIXED`는 공정성이 붕괴한다. 여성 8.1게임 / 남성 3.3게임, 편차 5.3. **절대 기본값으로 두면 안 된다.**
- `FREE`는 편차 1.2를 유지하면서 남복 51% · 혼복 42% · 여복 7%의 자연스러운 분포를 만든다. 3:1은 0%.

### D.2 3:1 감점 값 스캔 (출석 24명, `FREE`)

| `W_ODD` | 게임수 편차 | 남−여 격차 | 혼복 | 혼성(3:1) |
|---:|---:|---:|---:|---:|
| 0 | 0.9 | 0.51 | 9% | 20% |
| −70 | 0.9 | 0.41 | 20% | 16% |
| −120 | 0.9 | 0.45 | 33% | 10% |
| −180 | 1.2 | 0.38 | 41% | 1% |
| **−250** | **1.2** | **0.36** | **42%** | **0%** |
| −500 | 1.2 | 0.36 | 42% | 0% |

−250에서 3:1이 완전히 사라지고, 그 이상 키워도 변화가 없다(포화). 편차는 0.9 → 1.2로 0.3게임만 늘어난다. **이 0.3게임이 3:1 조합을 없애는 대가**다.

### D.3 출석 인원별 검증 (`FREE`, `W_ODD = −250`, 공정성 예외 ON)

| 출석 | 게임수 편차 | 남 평균 | 여 평균 | 남복 | 여복 | 혼복 | 혼성 |
|---|---:|---:|---:|---:|---:|---:|---:|
| 16명 (12/4) | 0.7 | 7.14 | 6.78 | 60% | 9% | 31% | 0% |
| 20명 (14/6) | 0.9 | 5.80 | 5.37 | 59% | 17% | 24% | 0% |
| 24명 (17/7) | 1.2 | 4.86 | 4.41 | 44% | 4% | 42% | 10% |
| 30명 (21/9) | 1.0 | 3.92 | 3.50 | 53% | 13% | 25% | 9% |
| 34명 (24/10) | 1.0 | 3.49 | 3.00 | 61% | 13% | 27% | 0% |

모든 구간에서 편차 ≤ 1.2, 남−여 격차 ≤ 0.5게임. 합격.

### D.4 여성 극소수 출석일 — 공정성 예외의 효과

여성이 1명뿐인 날은 3:1 말고는 조합이 불가능하다. 감점만 걸어두면 그 사람이 대기에 갇힌다.

| 출석 | 예외 규칙 | 게임수 편차 | 남 평균 | 여 평균 |
|---|---|---:|---:|---:|
| 20명 (19/1) | **없음** | 3.0 | 5.81 | **3.12** |
| 20명 (19/1) | **적용** | **1.3** | 5.72 | **4.80** |
| 20명 (17/3) | 적용 | 1.3 | 5.76 | 5.15 |
| 20명 (18/2) | 적용 | 0.9 | 5.71 | 5.32 |

예외 규칙(2게임 이상 뒤처지면 3:1 감점 면제)이 없으면 여성 1명의 게임 수가 남성 평균의 **절반 수준**으로 떨어진다. 이 규칙은 선택이 아니라 **필수 구현 항목**이다.

### D.5 결론 — 이 클럽의 권장 설정

```jsonc
{
  "genderPolicy": "FREE",       // 성별로 조합을 유도하지 않음
  "weights": { "odd": -250 },   // 3:1은 사실상 차단
  "oddRelaxThreshold": 2        // 단, 2게임 이상 뒤처지면 면제
}
```

**운영 팁**

1. 여복은 성비상 자동으로는 거의 안 나온다(0~17%). 여성 회원들이 여복을 원하면 **슬롯 유형 핀(F-409)** 으로 특정 대기 슬롯을 "여복"으로 지정하는 방식이 낫다. 자동 로직을 비트는 것보다 명시적으로 한 게임을 잡아두는 편이 예측 가능하다.
2. 혼복을 더 늘리고 싶으면 `PREFER_MIXED`로 바꾼다. 혼복이 42% → 59%로 오르고 남녀 게임 수도 거의 같아지지만, **여복은 0%가 된다.**
3. 여성 출석이 홀수인 날(3명, 5명, 7명…)은 한 명이 짝을 못 만나 대기가 길어지는 구간이 생긴다. 정상 동작이며, 게임 수 우선순위가 다음 회전에서 자동으로 보정한다.
4. 이 수치들은 **시뮬레이션 값**이다. 실제 3회 운영 후 세션 요약(F-604·F-606)의 유형 분포와 개인별 게임 수를 보고 `odd` 값과 정책을 재조정하는 것을 전제로 한다.


## 부록 E. 구현본 실측 (v1.0 코드)

명세서의 시뮬레이션(부록 D)은 대기열 없이 코트만 모델링한 것이다. 실제 구현본은 **대기 슬롯 7개를 거치는 구조**라 동작이 다르다. 아래는 배포한 코드 그대로 2시간 세션을 12회 반복한 결과다.

| 출석 | 경기 | 게임수 편차 | 남 평균 | 여 평균 | 여 최소 | 유형 분포 |
|---|---:|---:|---:|---:|---:|---|
| 16명 (12/4) | 29 | 1.1 | 7.14 | 7.08 | 6.7 | 남복 50% · 혼복 50% |
| 20명 (14/6) | 28 | 1.0 | 5.56 | 5.47 | 5.0 | 남복 41% · 혼복 59% |
| 24명 (17/7) | 29 | 1.2 | 4.81 | 4.74 | 4.0 | 남복 44% · 혼복 54% · 여복 2% |
| 30명 (21/9) | 29 | 0.9 | 3.80 | 3.81 | 3.3 | 남복 42% · 혼복 56% · 여복 2% |
| 34명 (24/10) | 28 | 1.0 | 3.37 | 3.28 | 3.0 | 남복 44% · 혼복 54% · 여복 2% |
| 20명 (19/1) | 29 | 1.7 | 5.78 | 4.50 | 4.5 | 남복 84% · 혼성 16% |
| 21명 (18/3) | 29 | 1.4 | 5.64 | 4.94 | 4.6 | 남복 72% · 혼복 23% · 혼성 5% |

**부록 D와 달라진 점**

- **혼복 비중이 올랐다(25~42% → 54~59%).** 3:1이 차단되고 여복은 여성 4명이 동시에 비어야 하므로, 여성이 **한 게임에 2명씩 배분되는 형태**로 수렴한다. 결과적으로 여성이 혼자 남거나 몰리는 일이 없다.
- **여복은 거의 나오지 않는다(0~2%).** 예상대로다. 여복을 원하면 슬롯 유형 핀(F-409)으로 지정해야 한다.
- **남녀 평균 게임 수가 거의 같아졌다**(격차 0.1 이하). 부록 D에서는 0.4~0.5 차이가 있었다.
- 여성 1명·3명 출석일은 편차가 1.4~1.7로 다소 크다. 짝을 맞출 수 없는 인원이 생기는 구조적 한계이며, 공정성 예외가 작동해 해당 인원도 4.5게임 이상 확보한다.

**검증 항목 통과 여부**

| 기준 | 결과 |
|---|---|
| 게임 수 편차 ≤ 1 | 대부분 통과 (여성 극소수일 때 1.4~1.7) |
| 직전 4인 3게임 내 재결합 0건 | **통과 (0%)** — 20·24·30명 각 180회 검증 |
| 3:1 경기 비율 ≤ 15% | 통과 (여성 1명 케이스만 16%) |
| 여성 평균이 남성 평균 −1 이내 | **통과** |
| 여성 1명 케이스 평균 −1.5 이내 | 통과 (4.50 vs 전체 5.72) |
